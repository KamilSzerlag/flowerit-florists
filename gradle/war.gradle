apply plugin: "war"

bootWar {
    mainClassName = "com.flowerit.florists.FloristsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
